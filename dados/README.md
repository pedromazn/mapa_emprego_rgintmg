# Dados

O repositorio publica a base tratada em `saldo_de_emprego_nas_regioes.csv`.

Formato esperado:

- uma linha por regiao e ano
- colunas `Regioes`, `Saldo` e `Ano`

Se preferir trabalhar com a planilha original, o script tambem aceita `saldo_de_emprego_nas_regioes.xlsx` na raiz do projeto.
